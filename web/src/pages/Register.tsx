import React from "react";
import { ReactElement } from "react";

import SignIn1 from "../components/sign-up/Sign-up";

const Register = (): ReactElement => {
  return (
    <React.Fragment>
      <SignIn1 content={null} />
    </React.Fragment>
  );
};

export default Register;
